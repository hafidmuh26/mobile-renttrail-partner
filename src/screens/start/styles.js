import { StyleSheet } from "react-native";

const styles = StyleSheet.create({

  instructions: {
    textAlign: 'center',
    color: 'black',
    marginBottom: 360,
    
  },
});

export default styles;
